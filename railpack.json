{
  "$schema": "https://schema.railpack.com",
  "provider": "staticfile",
  "caches": {
    "bun": {
      "directory": "/root/.bun/install/cache",
      "type": "shared"
    }
  },
  "steps": {
    "build": {
      "commands": [
        {
          "image": "oven/bun:1.3.11",
          "src": "/usr/local/bin/bun",
          "dest": "/usr/local/bin/bun"
        },
        "...",
        {
          "cmd": "bun scripts/materialize-deploy-links.ts",
          "customName": "Materialize shared files for the active variant"
        },
        {
          "cmd": "bun run build",
          "customName": "Build the active variant (set LISIBLE_VARIANT to override)"
        }
      ],
      "variables": {
        "ASTRO_TELEMETRY_DISABLED": "1",
        "CI": "true"
      },
      "caches": ["bun"],
      "deployOutputs": [
        {
          "include": ["Caddyfile", "dist"]
        }
      ]
    }
  },
  "deploy": {
    "inputs": [
      {
        "step": "packages:caddy",
        "include": ["/railpack/caddy"]
      },
      {
        "step": "build",
        "include": ["Caddyfile", "dist"]
      }
    ],
    "variables": {
      "LANG": "en_US.UTF-8",
      "LC_ALL": "en_US.UTF-8"
    }
  }
}
